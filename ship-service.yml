spring.application.name=ship-service


server.port=8787

# ??a ch? Eureka Server
eureka.client.service-url.defaultZone=http://localhost:8686/eureka/

# Cho phép ??ng ký
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Rest template vs eureka (không tham s? và có tham s?) : nghiên c?u